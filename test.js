const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto('http://localhost:5173'); await page.click('.ph'); await new Promise(r => setTimeout(r, 1000)); await page.click('#btn-dir-r'); await new Promise(r => setTimeout(r, 500)); const active = await page.$eval('#btn-dir-r', e => e.className); console.log('Class is:', active); await browser.close(); })();
